# Cortex

Cortex is a knowledge service that exposes retrieval and ingestion APIs over an OKF bundle. It does not contain an LLM agent; instead, it provides structured tools for external agentic systems to query, navigate, and ingest knowledge.

## Language

**OKF (Open Knowledge Format)**:
Google's vendor-neutral format for representing knowledge as markdown files with YAML frontmatter. Version 0.2 as of 2026.
_Avoid_: OFK, open knowledge standard

**Bundle**:
A directory tree of OKF concept files. The source of truth for all knowledge Cortex serves.
_Avoid_: knowledge base, corpus, collection

**Concept**:
A single `.md` file with YAML frontmatter (`type` required) and markdown body. The atomic unit of knowledge in an OKF bundle.
_Avoid_: document, file, page

**Concept path**:
The bundle-relative path without `.md` (e.g. `tables/orders`). Serves as the concept's unique identity across the system.
_Avoid_: document id, file path

**index.md**:
A reserved OKF file that enumerates directory contents for progressive disclosure. Generated by Cortex during ingestion.
_Avoid_: table of contents, listing

**Knowledge service**:
Cortex's architectural role: provides search, read, list, ingest, and validate endpoints. No LLM agent inside.
_Avoid_: agent, orchestrator

**BundleStore**:
An abstraction over the storage backend (local filesystem or GCS bucket) that Cortex reads bundles from.
_Avoid_: storage backend, file system

**Ingest**:
The process of walking a directory of `.md` files, parsing OKF frontmatter, embedding concept bodies into Chroma, and regenerating `index.md` files. Triggered explicitly by the user.
_Avoid_: index, sync, refresh

**Embedding provider**:
An abstraction over the service that generates vector embeddings for concept bodies. Config-switchable: sentence-transformers (local), ollama, or Vertex AI.
_Avoid_: embedding model, vectorizer

**Bundle root**:
The top-level directory of the OKF bundle that a Cortex instance serves. Configured at startup; `POST /ingest` rescans sub-paths within it.
_Avoid_: bundle path, mount point

**Search modes**:
`POST /search` runs in one of three modes — `vector` (semantic), `lexical` (grep-like term match), or `hybrid` (both fused). Returns concept pointers, not answer content.
_Avoid_: retrieval strategy, RAG

**Files-first retrieval**:
The design stance that the Chroma index returns concept pointers and the agent reads full content from the bundle via `GET /concepts/{path}`. Answers stay grounded in source markdown.
_Avoid_: chunk-based RAG, vector-first RAG
